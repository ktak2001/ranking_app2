cloud run jobで実行できるような２つのスケジューラを用意したい。
１つ目は、set_youtubers.pyを毎日実行するようなものだ。
ただし、そこで呼び出す関数であるset_youtuber_superChatsにおけるget_videos_until_dateについて、現在はdefaultで2023,12,31になっているが、それをその日から5日前まで処理するように変更してほしい。

2つ目は、各vtuberの情報を週に一回updateするものだ。そのvtuberの画像や表示名は定期的に変更される可能性があるためである。

3つ目は、全supportersの情報を2週間に一回updateするものだ。同様に画像や表示名が変更されるためである。

現在のset_youtubers.pyと、set_youtuber_superChatsが書かれたadmin_tasks.pyを添付するので確かめてほしい

-------

更新用スクリプトまではちゃんと作れました。

あと念の為共有しておくこととして、現在スケジューラとは別にset_youtubers.pyをcompute engineで実行するスクリプトがこちらにあるのですが、色々と参考になるかもしれないので念の為添付しておきます。

ちなみにcompute engineで実行するスクリプトについてはdockerfileを使っていないのですが、cloud runだと必要になってくるのでしょうか。

あと、それぞれのjobについてですが、基本的には5時間程度で終わるものだと考えております。

------

gcloud builds submit --tag gcr.io/$PROJECT_ID/ranking-app-jobs

上のコマンドはdockerfileがあるディレクトリで直接叩けばいいですか?
また、実際のbackendディレクトリやscriptsディレクトリには、cloud run jobsはどのようにしてアクセスするのでしょうか（compute engineではVM上でgit cloneすることでアクセスしていましたが）

また、vscodeでdockerfileを開くと、tag recommendationsが出るのですが、3.13-slimにすればいいということでしょうか

また現在画像のようにUI上でschedulerを設定しようと試みているのですが、実行内容におけるターゲットタイプなどはどのように選択すればいいのでしょうか。それともUI上で設定するよりCLIで設定した方が楽でしょうか

-----

update_vtubers_profile.pyなどを作ったはいいものの、どこを経由してそもそもこのファイルが実行されるかが分からない。現在はscriptsディレクトリにただそのファイルを配置しているだけである。

gcloud scheduler jobs create http update_videos_everyday \
  --location asia-northeast1 \
  --schedule "0 3 * * *" \
  --uri "https://run.googleapis.com/v2/projects/$PROJECT_ID/locations/asia-northeast1/jobs/set-youtubers-job:run" \
  --http-method POST \
  --oidc-service-account-email scheduler-sa@$PROJECT_ID.iam.gserviceaccount.com

例えば上のコマンドだが、これはset-youtubers-jobというジョブが既に作成されていることが前提の上でのコマンドな気がするが、そういったジョブをそもそも作成すればいいかが分からない。

-----

https://www.googleapis.com/youtube/v3/videos?part=liveStreamingDetails,statistics,status,topicDetails,localizations,snippet,contentDetails&id=fKhGEY0I4Tc&key=AIzaSyCKum_aL-2IjZ7vqTdXCR36hTrJGFMUHik

-----

現在、admin_tasks.pyのset_youtuber_superchatsという関数を実行することでfirestoreに初期値のデータを入れているのですが、たびたびエラーが起きて中断します。現状のままだと、中断したところからの再開ができないです。
具体的にエラーが起きる箇所は、
1. get_videos_until_date
2. get_video_details
3. update_for_each_video内のget_superchats_with_retry
4. update_supporter内のget_supporter_custom_url
です。
a. まず、各videoについて完全に終了した場合、それをdb.collection("youtubers").document(youtuber_id)のvideoIds配列に入れることで、1・2・3のエラーで中断した場合には対応できると考えます(具体的にはset_youtuber_superChats内のupdate_for_each_video関数呼び出し後に)。
b. 4のエラーの場合、これをエラーと見なさなければいいと考えます。具体的には、仮にget_supporter_custom_url内でエラーが起きた場合、それをupdate_supporter内でcatchし、supporter_custom_urlの文字列を"__error_getting_custom_url__"にして処理を続行すればいいはずです。このようにすれば、恐らく大丈夫だと思うので、aとbの処理をプログラムに埋め込んで下さい

-----
1.
bash deploy.sh
を実行する場合と、
gcloud builds submit --config cloudbuild.yaml
を実行する場合でどう違いますか。

2.
現在、firebaseにhostingしている状態です。一方、opensuperchat.comというドメインをcloudflareで取得しそこからそのhostingしているサイトを開きたいと考えております。現在画像のような状況です。ここからどのようにして目的を達成すればいいでしょう

-----

ドメインのネームサーバーとはなんでしょうか。そんなもの自分はもっていません。ドメイン関連はfirebase hostingとcloudflareのみに完全委託したいのですが、、

あと指示通りカスタムドメインを追加しようとしているのですが、この画像のような画面になりました。その後どうすればいいでしょう

-----

そもそもまだこのドメインを購入したことがないんですよね。
あとfirebaseでカスタムドメインを追加しようとした際、リダイレクト先のドメインを入力する、という項目も存在したのですが、こちらは何を入力するべきだったのでしょうが。

-----

現在、HeaderにYoutubeLoginというボタンを追加している。
これは、googleのメールアドレスで無料登録後、さらに自身のYoutubeチャンネルを紐づけるものである。しかし、このYouTubeログインボタンを押した際、色々と変な挙動を引き起こすことがある。

1. まず、YouTubeLoginを押したら順調に進むが、「このアプリはGoogleで確認されていません」というページが出てしまう。しかも私の個人的なメアドがデベロッパーとしてさらされている(安全ではないページに移動、を押せば一応次に遷移できる)
2. 次に、「project208...がGoogleアカウントへの追加アクセスを求めています」というページに遷移する。このページもできれば無くしてほしいし、無理であってもせめて「project-208...」ではなく「opensuperchat.com」に変えてほしい。
3. redirectされたとき、リロードが行われないため、user.supporterId == trueなのにも関わらず、右上のボタンが未だにYouTubeLoginになっている（本来マイページになっていてほしい）

youtubeLoginコンポーネントが使っているauth.jsも添付したのでそれらを踏まえて私の現状のYoutubeLoginの流れを解析してください。
その上で、現状の方法ではなく「OAuth 2.0 PKCE + Authorization Code Flow」が推奨される理由が知りたい。
また、同意途中に出てくる「project208...がGoogleアカウントへの追加アクセスを求めています」という画面については、どうしても無くせないのでしょうか。無理なら大丈夫ですが。

-----

get_video_details関数内の、
if "items" not in data or not data["items"]:
について、これは
if "items" not in data:
では駄目なんでしょうか。

また、set_youtuber_superChatsにおけるget_video_details関数の呼び出しについて、Noneが返ってきた場合、
db.collection("youtubers").document(youtuber_id)に、retrieveできなかったvideoIdの配列を作り(unneccesaryVideoIdsと同じ様に)、そこにそのvideoIdをpushした後continueしてほしい（今のプログラムだと、Noneが返ってきたらエラーでプログラムが終わってしまう。）

-----

現在oAuth同意画面を編集しようとしています。
そこで、サポートメールを選択しようとしているのですが、kazuki.takehi@gmail.com以外が選択できません。どうすればいいでしょう。
また、OpenSuperChatのロゴを作りたいです。OpenMoneyのロゴの画像を添付するのでこれのOpenSuperChat版を作ってほしい。

-----

一応opensuperchat@googlegroups.comを選択できるようになりましたが、ユーザの問い合わせはここに入ってくるという認識であってますか。またデベロッパの連絡先としてinfo@opensuperchat.comのようなメアドを作りたいと考えているのですが、これは有料なのでしょうか。それが無理として、gmailでなくても可能なのでしょうか。もう自分の電話番号でメアドを作りすぎて、これ以上は無理みたいなので

-----

youtubeLoginの再実装(PKCEフロー)を分かりやすく教えて

----

現在のlayout.jsに上手く統合するようにしてほしい。(app/layout.js)
また、api用ロジックはutils/api.jsというファイルにあるので参考にしてみて。

あと、frontendディレクトリをzipファイルにする方法教えて（node_modulesと.firebaseを除く）

-----

ありがとうございます、一応frontendディレクトリ全体のzipファイルも与えるので、それを踏まえて今回のpkceフローに対してより最適化ができるのであればお願いします。（特にないならいいですが）

-----

もしかして先程与えたzipファイルを解凍して中身を見ることができないのでしょうか。先ほどの解答は、中身を見た上での解答でしょうか。そうでないのなら、改めてzipファイルを解凍したうえで質問に答えてほしい

-----

・YoutubeLoginButtonを押した際何が起こるか、丁寧に解説してくれませんか。
・process.env.NEXT_PUBLIC_YT_CLIENT_ID
には何の値を入れておくべきなのですか
・現状作成したページは、
youtube_callback/page.js
api/oauth2callback/route.js
api/youtube/route.js
のみですが、他に作成するべきページ（あるいは要らないページ）はありますか。
・route.jsとはなんですか。page.jsとは役割が違うのでしょうか。
・api/youtube/refresh.jsや、youtube/callback/page.jsはまだ作成していないのですが、必要でしょうか

-----

youtubeloginButton.jsとyoutube_callback/page.js
を添付するので中身を見てほしいのですが、つまりyoutube/callback/page.jsを作らなくても、new URLSearchParamsのredirect_uriのyoutube/callbackの箇所をyoutube_callbackに変更するのみで十分のみなのでしょうか。

また、一応この一連のフローは、
(・まずユーザに通常のgoogleアカウントでログインしてもらう)
・その上でYoutubeログインボタンを押し、改めて自身のどのチャンネルをそのgoogleアカウントに紐づけるか選択してもらう
・紐づいたら、dbのuser=>{userId}のsupporterIdの値を、そのチャンネルのチャンネルIdにした上で、supporterIdにおけるconnectedUserの値をuserIdに変更する。
です。

この目的は、今回作成したYoutubeLoginButtonを押すことで達成されるでしょうか。

-----

上記エラーを直してほしい。
youtubeauthProvider.jsとそれをimportしているlayout.jsを添付するので中身を見た上で修正して

-----

画像のようなエラーが出たのですが、
oauthの承認済みリダイレクトurlにyoutube/callbackを載せておけばおそらく問題ないですよね

-----

現在firebaseConfig.jsという今添付したファイルがあるのですが、それとは別に新たにfirebase関連のファイルを作れということでしょうか。それともまとめて一緒にファイルにしたほうがいいでしょうか

-----

では、FB_PROJECT_IDなどの環境変数を定義する.envファイルもapi用に作るのでしょうか

それともfrontend直下にある.envファイルにまとめていい?

-----

.envの環境変数の最初にNEXT_PUBLICとつけるのとつけないのでどう挙動が変わるのでしょうか

-----

結局下のコードにしました。
ちなみに、何度試しても必ず!auth.currentUser == trueとなるんですよね。さっきgoogleでログインしていたばかりなのに。そういった原因が考えられるでしょうか。

またちなみにHeader.jsではlib/auth.jsで定義したuseAuth関数の呼び出しによってuserを取得しているのですが、その方法は駄目なのでしょうか。Header.jsで常にwrapしているため、二回呼び出すことになるのはまずいでしょうか。

-----

ありがとうございます。最終的に、戻ってきたサイトで自動でリロードされれば完璧です。リロードしないとHeaderのYoutubeLoginボタンがマイページボタンに変わらないので

-----

このアプリケーションのfrontendを
firebase deploy --only hosting
でdeployした場合、環境変数をどのように取得しているかが知りたいです。
ローカルと同様に.envファイルを見るのでしょうか。

また、firebase.jsonやnext.config.mjsはどんな役割を担っているのでしょうか。

今回新しくFB_CLIENT_EMAILとFB_PRIVATE_KEYという環境変数を追加したので、それらをdeploy時にもちゃんと読み取られてるかが知りたい。一応.envファイルや.env.productionファイルには定義してありますが、、

-----

ちなみに、firebaseConfigは以前から存在するファイルで上手く環境変数を読み取っていました。firebase.jsonに特に定義していないのに。なぜでしょうか。

-----

追加したのですが、また同じようなエラーが出ました。なぜでしょう。

ちなみに、他のページ（例えばyoutubers/[youtuberId]）などは例えば添付したファイルのように書いていますが、同じようにしなくて問題ないでしょうか（別にわざわざファイルを分離する必要はなさそうですが）。

-----

無料登録・ログインボタンを押したら上記のエラーが出たのですが、原因は何でしょうか。

-----

・マイページボタンに下線が引かれているのですが、それを無くしてほしいです。以下に貼り付けるHeader.jsを参考に修正して
・YoutubeLoginButtonのUIを、ログアウトやマイページと同じUIにして
・現在、Headerと主ページ（例えばClientHome.js）が被らない対策として、<div mt-5 pt-4></div>などを設定していますが、これだと、例えば画面の横サイズを小さくした場合、画像のように崩れてしまいます。画面がどのようなサイズになっても崩れないようにしてほしいです。

-----

opensuperchat.comでyoutubeLoginButtonを押すと、Googleのpopupは開かれるんですが、途中で画像のようなエラーが出てしまいます。どういった原因が考えられるでしょうか。

-----

方法2を選ぶことにします。
ただ以下の
firebase functions:secrets:set FB_PRIVATE_KEY "$(cat private_key.pem)"
ですが、cat private_key.pemで、実際のキーをいれることは可能なのでしょうか。

-----

指示通りにfb_private_keyを設定したのですが、同様に上記のエラーが出ました。
環境変数も以下に基づけばちゃんと設定されているはずです。なぜこのようなエラーが出るのでしょう。firebaseAdmin.jsも添付しておきます。

-----

上記はちゃんとsetされてるとみなせるでしょうか。
また、先程のgcloud run services describe ssrrankingappbf2df \
--region asia-east1 --format="value(spec.template.spec.containers[0].env)"で得た値に、NEXT_PUBLIC_WEB_URLなどが入ってなかったのですが、大丈夫でしょうか。route.jsなどではその値を使うので。

-----

環境変数ではないですね。やはりfirebaseAdmin.jsではinitializeAppを呼び出してないからですね。firebaseConfig.jsと同様に呼び出すべきな気がします。

-----

これはローカルではなくdeploy後の部分のnetworkですが、画像を見た上でエラー原因を探ってみて。ちなみに、initializeApp関連の問題はローカルでは解決されてるものの、deploy後のアプリではまだされてないはず

----

上のコードだと、router.refresh()が早すぎて、user.supporterIdが反映される前にrefreshしてしまって、結果ボタンがマイページにならない気がする。どうにかして、user.supporterId == ""の間はloadingさせておいて、user.supporterId != ""になった瞬間にrefreshするかトップページに遷移させることで、二回youtubeloginボタンを押させないようにしたい

-----

予想なんですけど、恐らくif文の中に入ってないから(admin.apps.lengthが0でない)initializeAppがされず、それでエラーが起きてたと思うんですよね。

一旦if文を外してbuildしてdeployしようと思うのですが、これによる弊害って何がありますか

-----

ちなみにdeployしたappによるログで、
console.log({admin})
console.log("apps", admin.apps)
の値はそれぞれ上記のようになりました。
つまりやはり、admin.appsはすでに中身のある配列になっていたようです。
原因としては何が考えられるでしょうか。
個人的には、以下のfirebaseConfig.jsが既に呼ばれてるからでは、と思いますが

ちなみにfirebaseConfigに関しては特にガードもせずにinitializeAppを読んでいますが、adminではなぜ駄目なのでしょう。

-----

youtubeログインボタンを押した後、youtube/callback/page.jsがapi/youtube/route.jsに委託してsupporterIdをdbにsetし、その後元のページ戻るような一連の流れになっているのかなと考えています。
しかし戻った際、Header.jsはuseAuthで瞬時にuser情報を入手するのですが、おそらくその時supporterIdはまだないはずです。なので、無理やりHeader.jsがuseAuthによって入手するuserのsupporterIdに適切な値を入れ、youtubeLoginButtonをマイページボタンに変えたいのですがどうすればいいでしょう。
また加えて、今は元のページに戻る仕様になっているのですが、そうではなくhref={`/supporters/${user.supporterId}`}に遷移するようにしてほしいです。お願いします。

useAuthがリアルタイムで更新されるようになったら負担が相当重くなりませんか。
リアルタイム性、このyoutube連携の時くらいですよ必要なの

dbを見て、user.supporetrId != ""だったらrouter.replaceするものだと思うのですが、なぜその時Headerは再度新しくuseAuthを呼び出さないのでしょうか。

>「リアルタイム監視＝常に重い」は誤解
>しかも 今回の listener は supporterId が入った瞬間に unsub() して切断 します。

・いえ、今回重くなるとはいってるのはyoutube/callbackにあるunsubではなく、useAuthの部分です。
・あと、パターンBですが、どうそれをすればいいのでしょう。Header.jsにsetUserはありますが、supporterIdの情報を先に知るのはyoutube/callback/page.jsですよね。

uCNiBJCk

-----

Case(1・2)で紹介されたヘッジファンド戦略（グローバルマルチ戦略）に関する具体的な情報、は添付したpdfにあります。その上でQ2に答えて

また、添付資料を元にした授業について、簡単なコメント（質問・要望・感想含む）をコメントシート用に書いてほしい